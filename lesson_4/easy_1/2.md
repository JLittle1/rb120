Within the class definitions for `Car` and `Truck`, put `include Speed` to mixin the `Speed` module. After that, you can test the `go_fast` instance method by calling it on any `Car` or `Truck` object.